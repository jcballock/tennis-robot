# Motor Type
odrv0.axis0.config.motor.motor_type = MOTOR_TYPE_HIGH_CURRENT

# Pole Pairs & Torque Constant
odrv0.axis0.config.motor.pole_pairs = 2
odrv0.axis0.config.motor.torque_constant = 0.0452

# Current Limits
odrv0.axis0.config.motor.current_lim = 10.4           # Rated Continuous Current (A)
odrv0.axis0.config.motor.calibration_current = 5.0   # Safe current for calibration routine (A)
odrv0.axis0.config.motor.requested_current_range = 15.0 # Sensor range setup (A)

# Velocity & Protection Limits
odrv0.axis0.config.controller.vel_limit = 65.0        # Max speed limit in turns/s (~3900 RPM)
odrv0.axis0.config.motor.torque_lim = 0.47            # Max continuous torque limit (Nm)


odrv0.axis0.encoder.config.cpr = 12
odrv0.axis0.encoder.config.mode = ENCODER_MODE_HALL

# Save & Reboot
odrv0.save_configuration()
odrv0.reboot()
